import { Component } from '@angular/core';

@Component({
  selector: 'app-cart',
  template: `
    <p>
      cart works!
    </p>
    <!-- small html elements  -->
  `,
  styles: [
  ]
})
export class CartComponent {

}
// ng g c --skip-tests --inline-template --inline-style 
// created using cli flags
// example for creating cli flags for the components